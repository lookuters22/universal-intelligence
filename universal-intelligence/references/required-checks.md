# Required Checks — internal gate only

Emit textual audit **only** if user asks; otherwise run mentally pre-final assistant message.

## Binary gates *(all PASS)*

### G1 Dogma grounding

≥3 grounded dogmas precede exploratory list; each exploratory tags ≥1 break. Fail if fabricated generic dogmas after ideas.

### G2 Forest integrity

Exactly 4–5 entries; exploratory family spread visible—not five cosmetic variants. Fail if slapstick / spectacle entropy dominates plausible **exploratory grain** unless brief demands pure chaos satire (**then** annotate user mandate).

### G3 Friction discipline

Complete somatic tables + systemic + Pain Coordinate (+ optional Grain) per exploratory; forbid premature fixes/synthesis merges.

### G4 Pivot singularity / equation fidelity

Exactly one `PRIMARY:` equation referencing `the-pivot.md`; hybrid averaging masquerading as pivot fails—even if linguistic harvest metaphor appears.

**Correct-road delta sub-check.** Pivot block includes **Correct-road delta** prose describing orthodox choke points that excise hinge early—shows dependence on wrong-road (per `the-pivot.md`). Absent ⇒ FAIL.

### Quarry-specific sub-check *(when PRIMARY = Friction-as-Quarry)*

Must identify **explicit micro-canon artifact** uplifted—not vague "creative vibes."

### G5 DNA + hinge visibility *(USER-SEE)*

Reader articulates hinge / quarry shard powering outcome—not decorative.

### G6 Relapse strip-test

Generic SaaS interchangeability disguised branding ⇒ restart Step 1 (not synonyms).

### G7 Tone / vocabulary sanitization sweep

Banned cluster absent (`anti-patterns.md`).

### G8 Forbidden lookahead

No step leaks artifacts barred by sequencing table (`SKILL.md`).

### G9 Teachability + control surfaces

Minimal inspectable causal chain **plus** `WHY-CHOO` (three rejected alternatives), `FORK-TAPE` checkpoints, **`LEARN` pedagogy stub** absent explicit user blackout on teaching length. Pure black-box or answer-only capsule ⇒ FAIL unless user forbade exposition/teaching payloads.

### G10 Ethics hard stops

Sexual minors, facilitation of violence, deceptive harm, illicit enablement ⇒ refuse outright—no loophole choreography.

---

## Retry policy

Earliest failing gate ⇒ rerun owning step only. Repeated double failures ⇒ deepen exploratory mis-role passes—not synonym polish cycles.
